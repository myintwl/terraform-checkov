name: build
on:
  pull_request

jobs:
 checkov:
    runs-on: ubuntu-latest
    name: bridgecrewio/checkov-action@master
    steps:
     - name : checkout repo
       uses: actions/checkout@master
       with: 
          directory: "."
          
     
